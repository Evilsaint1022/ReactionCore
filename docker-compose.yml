services:
  raspberry:
    build: .
    container_name: raspberry
    restart: unless-stopped
    environment:
      TOKEN: ${TOKEN}
      CLIENT_ID: ${CLIENT_ID}
      GUILD_ID: ${GUILD_ID}
    volumes:
      - /srv/raspberry/storage:/app/src/Utilities/Storage